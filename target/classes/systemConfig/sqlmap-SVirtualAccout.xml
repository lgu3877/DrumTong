<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 	
		"-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 시스템 가상계좌 테이블	 -->
<mapper namespace="com.drumtong.system.dao.SVirtualAccoutDAO">
	<insert id="insert" parameterType="SVirtualAccoutVO">
		INSERT INTO SVIRTUALACCOUNT 
			(MEMBERID, ESTID, SALECODE, POINT) 
				VALUES
			(#{memberid}, #{estid}, #{salecode}, #{point})
	</insert>
	
	<update id="refund">
		UPDATE SVIRTUALACCOUNT SET 
			STATUS='RETURN' 
				WHERE ESTID=#{estid} AND SALECODE=#{salecode}
	</update>
</mapper>
